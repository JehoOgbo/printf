
	{
		s = malloc(sizeof(char) * (a + 2));
		if (s == NULL)
		{
			free(s);
			return (NULL);
		}
	}
	else
	{
		s = malloc(sizeof(char) * (a + 1));
		if (s == NULL)
		{
			free(s);
			return (NULL);
		}
	}
